export const OFFERS_COUNT = 10;

export const enum PATHS_NAMES {
  Main = '/',
  Favorite = 'FAVOURITE',
  Login = 'LOGIN',
  Offer = 'OFFER',
  NotFound = '*',
}

export const enum AUTH_STATUS {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
